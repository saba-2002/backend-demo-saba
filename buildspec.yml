version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - cd /home/ubuntu/backend-demo-saba
      - npm install

  build:
    commands:
      - echo "Starting the server..."
      - node app.js

artifacts:
  files:
    - '**/*'
  base-directory: backend-demo-saba
